import { KeycloakAdminConfig } from './keycloak-admin-config';

describe('KeycloakAdminConfig', () => {
  it('should create an instance', () => {
    expect(new KeycloakAdminConfig()).toBeTruthy();
  });
});
